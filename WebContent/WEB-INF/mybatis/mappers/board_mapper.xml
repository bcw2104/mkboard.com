<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
														"HTTP://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.codeplayground.mapper.BoardMapperInterface">

	<resultMap type="boardDTO" id="boardDTO">
		<id column="board_id" property="boardId"/>
		<result column="board_name" property="boardName"/>
		<result column="category_id" property="categoryId"/>
	</resultMap>

	<select id="selectOnebyId" parameterType="String" resultMap="boardDTO">
		SELECT * FROM tbl_board  WHERE board_id = #{boardId}
	</select>

	<select id="selectListbyCategory" parameterType="String" resultMap="boardDTO">
		SELECT * FROM tbl_board  WHERE category_id = #{categoryId}	ORDER BY sort_order ASC
	</select>

</mapper>