<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
														"HTTP://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.inhaplayground.mapper.SequenceMapperInterface">

	<select id="next" parameterType="String" resultType="int">
		SELECT SEQ_${type}.NEXTVAL FROM dual
	</select>

</mapper>