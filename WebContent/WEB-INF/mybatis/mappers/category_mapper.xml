<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
														"HTTP://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.codeplayground.mapper.CategoryMapperInterface">

	<resultMap type="categoryDTO" id="categoryDTO">
		<id column="category_id" property="categoryId"/>
		<result column="category_name" property="categoryName"/>
	</resultMap>

	<select id="selectAll" resultMap="categoryDTO">
		SELECT * FROM tbl_category	ORDER BY sort_order ASC
	</select>

	<select id="selectOnebyId" parameterType="String" resultMap="categoryDTO">
		SELECT * FROM tbl_category WHERE category_id = #{categoryId}
	</select>

</mapper>